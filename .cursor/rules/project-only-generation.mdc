---
description: 仅在本项目工作区内生成与修改代码与文件
alwaysApply: true
---

# 仅在本项目中生成

## 范围

- **所有新建、编辑、移动或建议创建的文件**必须位于当前 Cursor 工作区根目录之下，使用工作区内的相对路径。
- **不要**在回复或补丁中把输出写到工作区外的绝对路径（例如其他盘符、用户主目录下的任意文件夹、`../` 跳出仓库等），除非用户**明确**要求写到指定外部路径。
- **不要**主动建议「把项目拆到工作区外」「在别的目录另建一份工程」等；若用户未说明外部路径，默认一切生成都留在本仓库内。

## 命令与引用

- 终端命令、脚本与配置中涉及路径时，优先使用**相对于工作区根**的路径；避免假设工作区之外的目录结构。
- 文档或注释里若要举例路径，用项目内占位路径（例如 `src/…`），勿用与工作区无关的示例绝对路径作为主要方案。

## 例外

仅在用户**逐条明示**例外时（例如「写到 `D:\backup\…`」「同步到 sibling 文件夹」），才可以针对该次指令操作或提及工作区外路径。
